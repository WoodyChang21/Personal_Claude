# Secrets
.env
**/.env

# Python environments
.venv/
**/.venv/
__pycache__/
**/__pycache__/
*.pyc
*.pyo

# Local MCP server installations
**/.mcp-servers/

# Machine-specific Claude settings
**/.claude/settings.local.json

# Cursor IDE
.cursor/
**/.cursor/

# Stack dumps
*.stackdump

# Personal resume (keep local only — not for public repos)
**/resume/*.pdf
**/resume/*.aux
**/resume/*.log
**/resume/*.out
**/resume/*.synctex.gz

# Tailored resume outputs (local only — generated per job application)
**/resume/tailored/
